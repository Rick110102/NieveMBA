/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var geometry = 
    /* color: #d63000 */
    /* shown: false */
    ee.Geometry.MultiPoint(
        [[-70.09894723108494, -17.00299993587547],
         [-69.7935191004036, -17.45951097232333]]);
/***** End of imports. If edited, may not auto-convert in the playground. *****/

// Enmascararemos con el acumulado, claro, perderemos cobertura glaciar, quiza, pero hey, tenemos el filtro
// filtro de glaciares puros, cosa que, si eliminamos glaciar por enmascarar agua, corregimos con la capa de glaciar
// Glaciar dry, así que no nos preocupemos por enmascarar con el acumulado de agua!

 // TEMA TRANSVERSAL GLACIARES
// Son 48 las regiones a procesar
// enumeardas de 1 a 48
var params = {
    'regionGlacier': 4,
    'pais': 'PERU', // Options: 'PERU', 'BOLIVIA', 'ECUADOR', 'COLOMBIA ,'VENEZUELA'
    'shadowSum':3500,
    'trees': 50,
    'samples': [
            3000,              // glacier  3000
            8000               // no-glacier 8000
          ],
    'versionOutput':1,   // todos 4, en caso de 1007 version 5
    'versionBatch':4,
    'exportar': true,  // false, true
    'cloud': 170  // fraccion cloud de SMA para considera un pixel como no observado
};


// Ver si optamos por el estimado de median...
// Ya que el wet, es demasiado ruidoso...
var variables = [
                    "blue_median_wet",
                    "green_median_wet",
                    // "green_min",
                    "red_median_wet",
                    "nir_median_wet",
                    "swir1_median_wet",
                    "swir2_median_wet",
                    // "ndwimf_median",
                    "ndwimf_median_wet",
                    "ndsi_median_dry",
                    "ndsi_median_wet",
                    "ndsi_min",
                    // "ndsi_median",
                    // "blue_median",
                    // "green_median",
                    // "red_median",
                    // "nir_median",
                    // "swir1_median",
                    // "swir2_median",
                    'slope',
                    "gv",
                    "npv",
                    "soil",
                    "cloud",
                    "snow",
                    "shade"
              ]

              
//l5(1985-2012), l7(1999-2022), lx(1985-2013), l8(2013-2022)
//             YEAR, SENSOR,  <=Nube, >=nir_median,>=red_median
var YearsSensors =  [
              // [1985,  'lx',      75,    2114,      2497], 
              // [1986,  'lx',      75,    2114,      2497], 
              // [1987,  'lx',      75,    2114,      2497], 
              // [1988,  'lx',      75,     2114,      2497], 
              // [1989,  'lx',      75,     2114,      2497], 
              // [1990,  'lx',      75,     2114,      2497], 
              // [1991,  'lx',      75,     2114,      2497], 
              // [1992,  'lx',      75,     2114,      2497], 
              // [1993,  'lx',      75,     2114,      2497], 
              // [1994,  'lx',      75,     2114,      2497], 
              // [1995,  'lx',      75,     2114,      2497], 
              // [1996,  'lx',      75,     2114,      2497], 
              // [1997,  'lx',      75,     2114,      2497], 
              // [1998,  'lx',      75,     2114,      2497], 
              // [1999,  'lx',      75,     2114,      2497], 
              [2000,  'lx',      75,     2114,      2497], 
              [2001,  'lx',      75,     2114,      2497], 
              [2002,  'lx',      75,     2114,      2497], 
              [2003,  'lx',      75,     2114,      2497], 
              [2004,  'lx',      75,     2114,      2497], 
              [2005,  'lx',      75,     2114,      2497], 
              [2006,  'lx',      75,     2114,      2497], 
              [2007,  'lx',      75,     2114,      2497], 
              [2008,  'lx',      75,     2114,      2497], 
              [2009,  'lx',      75,     2114,      2497], 
              [2010,  'lx',      75,     2114,      2497], 
              [2011,  'lx',      75,     2114,      2497], 
              [2012,  'l7',      75,     2114,      2497], 
              [2013,  'l7',      75,     2114,      2497], 
              [2014,  'l7',      75,     2114,      2497], 
              [2015,  'l7',      75,     2114,      2497], 
              [2016,  'l7',      75,     2114,      2497], 
              [2017,  'l7',      75,     2114,      2497], 
              [2018,  'l7',      75,     2114,      2497], 
              [2019,  'l7',      75,     2114,      2497], 
              [2020,  'l7',      75,     2114,      2497],
              [2021,  'l7',      75,     2114,      2497],
              [2022,  'l7',      75,     2114,      2497],
              [2023,  'l7',      75,     2114,      2497],
              [2024,  'l7',      75,     2114,      2497],
              [2013,  'l8',      75,     1730,      2304], 
              [2014,  'l8',      75,     1730,      2304], 
              [2015,  'l8',      75,     1730,      2304], 
              [2016,  'l8',      75,     1730,      2304], 
              [2017,  'l8',      75,     1730,      2304], 
              [2018,  'l8',      75,     1730,      2304], 
              [2019,  'l8',      75,     1730,      2304], 
              [2020,  'l8',      75,     1730,      2304],
              [2021,  'l8',      75,     1730,      2304],
              [2022,  'l8',      75,     1730,      2304],
              [2023,  'l8',      75,     1730,      2304],
              [2024,  'l8',      75,     1730,      2304],
              [2025,  'l8',      75,     1730,      2304], // Nuevo año a la colección de glaciares
              [2022,  'l9',      75,     1730,      2304],
              [2023,  'l9',      75,     1730,      2304],
              [2024,  'l9',      75,     1730,      2304],
              [2025,  'l9',      75,     1730,      2304], // Nuevo año a la colección de glaciares
            ]  
             

/**
* Importa geometria: carta y region
*/
 
var AssetRegions = 'projects/ee-mapbiomasaguaperu/assets/RegionesGlaciaresWet';
// var AssetRegionsRaster = 'projects/mapbiomas-peru/assets/LAND-COVER/AUXILIARY-DATA/RASTER/clasificacion-regiones-glacier-5';
var AssetClasBatch = 'projects/mapbiomas-peru/assets/LAND-COVER/COLLECTION3/GLACIER/classification'
var coll_glacier = ee.ImageCollection("projects/mapbiomas-raisg/PRODUCTOS/AGUA/COLECCION01/glacier-integracion-03");
var palettes = require('users/mapbiomas/modules:Palettes.js');
var dem = ee.Image("USGS/SRTMGL1_003");
var slope = ee.Terrain.slope(dem).rename('slope');


var region = ee.FeatureCollection(AssetRegions).filterMetadata('ord', 'equals', params.regionGlacier)

var regionRaster = region.reduceToImage({
    properties: ['id_regionC'],
    reducer: ee.Reducer.first()
}).gt(0).selfMask();


var clasCollBatch = ee.ImageCollection(AssetClasBatch).filter(ee.Filter.eq('version', params.versionBatch));


var Clasificacion_last_coll = ee.Image(0).select([]);
ee.List.sequence(1985, 2025, 1).getInfo().forEach(function(year){
  var GlacierYear = coll_glacier.filterMetadata('year', 'equals', year).mode();
  Clasificacion_last_coll = Clasificacion_last_coll.addBands(GlacierYear.rename('classification_'+year));
});
print(Clasificacion_last_coll);

var Clasificacion_last_coll = Clasificacion_last_coll.unmask(25).updateMask(regionRaster)


// Map.centerObject(region, 10);
Map.addLayer(
    region.style({ fillColor: '000000', color: '00000000'}), 
    {}, 'Background'
);

/**
* Importa el modulo 'GetImages' para la obtención de los mosaicos
*/
// NUEVO
var getImages = require('users/mapbiomasaguaperu/GlaciaresWet2026:New_Collection/GetImages.js');
var smafractions = require('users/mapbiomasaguaperu/GlaciaresWet2026:New_Collection/Sma-fraction.js');
/**
 * Get stable pixels
 * Get stable pixels from mapbiomas collection 2
 * Then cross over reference datasets 
 */
var getStablePixels = function (image, classes) {
  
  var bandNames = image.bandNames(),
      images = [];

  classes.forEach(function(classId){
      var previousBand = image
        .select([bandNames.get(0)]).eq(classId);
          
      var singleClass = ee.Image(
        bandNames.slice(1)
          .iterate(
            function( bandName, previousBand ) {
              bandName = ee.String( bandName );
              return image
                .select(bandName).eq(classId)
                .multiply(previousBand);
            },
            previousBand
          )
      );
      
      singleClass = singleClass
        .updateMask(singleClass.eq(1))
        .multiply(classId);
      
      images.push(singleClass);
  });
  
  
  // blend all images
  var allStable = ee.Image();
  
  for(var i = 0; i < classes.length; i++) 
    allStable = allStable.blend(images[i]);

  return allStable;
};

var getSamples = function(reference, mosaic, points) {
  
    var training = reference
      .addBands(mosaic)
      .sampleRegions({
          collection: points,
          properties: ['reference'],
          scale: 30,
          geometries: true,
          tileScale: 4
    });
    
    return {
      points: points, 
      training: training 
    };
    
};


// Pixel estable
 var classes = ee.List.sequence(1, 34).getInfo();
 var stablePixels = getStablePixels(Clasificacion_last_coll, classes);
 
 stablePixels= stablePixels.updateMask(regionRaster)
                           .rename("reference");
 Map.addLayer(stablePixels,{},'stablePixels',false)
// clasificacion random forest

  var points = stablePixels
  .addBands(ee.Image.pixelLonLat())
  .stratifiedSample({
      numPoints: 0,
      classBand: 'reference',
      region: region.geometry().bounds(),
      scale: 30,
      seed: 1,
      geometries: true,
      dropNulls: true,
      classValues: [34, 25], 
      classPoints: [ params.samples[0], params.samples[1] ]
});


//var Clasification = ee.Image();
YearsSensors.forEach(
  function(yearsensor) {
    var tree = {
      nir_dry:yearsensor[3],  //mayor o igual a
      red_dry:yearsensor[4],  //mayor o igual a
    }
    var param = {
    // 'grid_name': params.grid_name,
    't0': yearsensor[0]+'-01-01',
    't1': yearsensor[0]+'-12-31',
    'satellite': yearsensor[1],
    'cloud_cover': yearsensor[2],
    'pais': params.pais,
    'regionMosaic': params.regionGlacier,
    'shadowSum':params.shadowSum
    };
  var ImagesYear = getImages.getImages(param,[],region);
  
  // print('Mosaic:'+yearsensor[0], ImagesYear[1]);
    
  var Mosaic = ImagesYear[0].updateMask(regionRaster)
                            .addBands(slope);
                            
  Mosaic = Mosaic.updateMask(Mosaic.select('blue_median').gte(0));
  
  Mosaic = smafractions.getFractions(Mosaic);
  
  var MosaicForClass = Mosaic.select(variables)
  print(yearsensor[0], MosaicForClass.bandNames().size())
  
  var trainingSamples = getSamples(stablePixels, MosaicForClass, points);
  var training = trainingSamples.training;
  print(training.size())
  training = training.filter(ee.Filter.and(ee.Filter.eq('reference', 34), ee.Filter.lte('snow', 15)).not())
                      .filter(ee.Filter.and(ee.Filter.eq('reference', 25), ee.Filter.gte('snow', 50)).not())
  print(training.size())
  
  // Define classifier
  var classifier = ee.Classifier.smileRandomForest({
      numberOfTrees: params.trees, 
      variablesPerSplit: 1
  });
  
  var _classifier = classifier.train(training, 'reference')
  var classified_RF = MosaicForClass.classify(_classifier)

  var classified_RF_v4 = classified_RF.where(Mosaic.select('nir_median').lte(tree.nir_dry),25)
                                      .where(Mosaic.select('red_median').lte(tree.red_dry),25)
                                      // .where(Mosaic.select('snow').lte(10),25)
                                      // .where(Mosaic.select('snow').gte(0).and(Mosaic.select('slope').gte(15)), 34)
                                      // .where(Mosaic.select('blue_median_dry').gte(1000), 34)
                                      // .where(Mosaic.select('green_median_dry').gte(1400), 34)
                                      // .where(Mosaic.select('red_median_dry').gte(1700), 34)
                                      // .where(Mosaic.select('nir_median_dry').gte(1900), 34)
                                      // .where(Mosaic.select('swir1_median_dry').lte(2300), 34)
                                      // .where(Mosaic.select('swir2_median_dry').lte(2100), 34)
                                      // .where(Mosaic.select('ndsi_median_dry').gte(164), 34)
                                      // .where(Mosaic.select('ndsi_median_wet').gte(188), 34)
                                      // .where(Mosaic.select('slope').gte(15), 34)
                                      // .where(Mosaic.select('soil').lte(25), 34)
                                      .where(Mosaic.select('ndsi_min').gte(100).add(Mosaic.select('snow').gte(20).and(Mosaic.select('slope').gte(0))), 34)
                                      // .where(Mosaic.select('snow').gte(170).and(Mosaic.select('slope').gte(5)), 34)
                                      // Rango observado para 
                                                // blue_median_dry: > 4,000 (valores típicos alrededor de 16,022)
                                                // green_median_dry: > 4,200 (rango observado 4,292-12,665)
                                                // red_median_dry: > 4,200 (rango observado 4,237-8,484)
                                                // nir_median_dry: > 3,200 (rango observado 3,237-12,338)
                                                // swir1_median_dry: < 1,300 (rango observado 536-1,257)
                                                // swir2_median_dry: < 1,000 (rango observado 357-978)
                                                // ndwimf_median_dry: > 100 (rango observado 100-110)
                                                // ndsi_median_dry: > 164 (rango observado 164-188)
                                                // ndsi_median_wet: > 188 (rango observado 188-195)
                                                // ndsi_min : 99 - 188
                                                // slope: Rango amplio (observado 5.016-34.286)
                                                // cloud: 175 - 223
                                                // snow:203 - 255
                                      // .where(Mosaic.select('shade').lte(256), 34)
                                      // .where(Mosaic.select('ndsi_min').gte(87), 34)
                                      .where(Mosaic.select('cloud').gte(params.cloud),27).selfMask();
                               
  var Collection = ImagesYear[1];
  //print('# imagenes:'+yearsensor[0], Collection.size())
   Map.addLayer(Mosaic.select(['swir1_median','nir_median','red_median']), {
          bands: 'swir1_median,nir_median,red_median',
          min:300,
          max:5000
      },'Mosaic-median-'+yearsensor[0]+ '-' +yearsensor[1], false)

    Map.addLayer(MosaicForClass, {
          bands: 'swir1_median_wet,nir_median_wet,red_median_wet',
          min:300,
          max:5000
      },'Mosaic-maxsnow-'+yearsensor[0]+ '-' +yearsensor[1], false)
      
    Map.addLayer(Mosaic, {
          bands: 'snow',
          min:0,
          max:100,
          palette:[ "#00007F",  "#002AFF",  "#00D4FF",  "#7FFF7F",  "#FFD400",  "#FF2A00",  "#7F0000"]
      },'fraction-'+yearsensor[0]+ '-' +yearsensor[1], false)
      
  // Map.addLayer(Mosaic.select('snow').gte(40).and(Mosaic.select('slope').gte(15)).selfMask(), {palette:'blue'}, 'snow',false)
  
  Map.addLayer(classified_RF_v4.updateMask(classified_RF_v4.eq(34)).selfMask(),{palette: 'ff0000'},'classified-RF-'+params.versionOutput+'-'+yearsensor[0],false)
  
  // Map.addLayer(classified_RF_v4,{min:25,max:34,palette: ['#ffa500','#9d9d9d','#0cd2ff']},'classified-RF-v4-'+yearsensor[0],false)
  // if(yearsensor[0]<=2025){
  // var clasifyearBatch = clasCollBatch.filter(ee.Filter.eq('year', yearsensor[0])).mode()
  
  // Map.addLayer(clasifyearBatch.updateMask(clasifyearBatch.eq(34)).selfMask(),{palette: 'fff729'},'classified-RF-' + params.versionBatch + '-batch'+yearsensor[0],false)
  // }

  // Map.addLayer(points,{},'points',false)
  
  // Map.addLayer(points)
  var pts = ee.FeatureCollection(points);
  var palettes = require('users/mapbiomas/modules:Palettes.js').get('classification8');
  // Layers
    var eeColors = ee.List(palettes);
    
    var trainingPointsColor = pts.map(
        function (feature) {
    
            var c = feature.get("reference");
    
            return feature.set({
                "style": {
                    "color": eeColors.get(c),
                    "pointSize": 4
                }
            });
        }
    );
    
    Map.addLayer(trainingPointsColor.style({
        "styleProperty": "style"
    }), {}, 'points',false);
    
    // RANDOM FOREST
  var Clasification_RF = classified_RF_v4.rename('classification') //Clasification.addBands(Glacier.rename('clasification'+yearsensor[0]))
  
      Clasification_RF = Clasification_RF.toInt8().set('region', params.regionGlacier)
                                 .set('year', yearsensor[0])
                                 .set('version', params.versionOutput)
                                 .set('pais', params.pais)
                                 .set('satellite', param.satellite)
                                 .set('metodo', 'RF-DT');
  // print(Clasification_RF)
  
  var sensornameupper =param.satellite.toUpperCase()
  
  if(params.exportar){

    Export.image.toAsset({
      image: Clasification_RF,
      description:'GLACIER-WET-' + sensornameupper + '-' + params.regionGlacier + '-'+ yearsensor[0] + '-'+ params.versionOutput,
      assetId:'projects/mapbiomas-peru/assets/LAND-COVER/COLLECTION4/GLACIER-WET/classification/'+ 
              'GLACIER-WET-' + sensornameupper + '-' + params.regionGlacier+ '-'+ yearsensor[0]+ '-'+ params.versionOutput,
      scale: 30,
      pyramidingPolicy: {
        '.default': 'mode'
      },
      maxPixels: 1e13,
      region: region.geometry().bounds()
    });
    

   }
  } )


/**
* Despliega en el mapa los mosaicos y polígonos necesarios
* para la visualización
*/
//print(Clasification)



Map.addLayer(
    region.style({ fillColor: '#ff000000', color: 'f59e42'}),
    {}, 'Regions ' + params.pais, false
);


// Comprobando la clasificación


// Map.addLayer(image,{min:25, max:34}, 'Imagen85Rick');
// var image85 = ee.Image('projects/mapbiomas-raisg/TRANSVERSALES/PERU/COLECCION4/GLACIAR/clasificacion/GLACIER-LX-1013-1985-4');
// Map.addLayer(image85,{min:25, max:34}, 'Imagen85IBC');


// Año 2000
// var image00 = ee.Image('projects/mapbiomas-raisg/TRANSVERSALES/PERU/COLECCION4/GLACIAR/clasificacion/GLACIER-LX-1013-2000-4');
// Map.addLayer(image00,{min:25, max:34}, 'Imagen00IBC');

// Map.addLayer(image2, {min:25, max:34}, 'Imagen00Rick');